declare{
	student;
	(int) = foo(student s);
float average = 0;
}
type student{
	public int sum = 100, count=5.5;
	public (float, int) = function average(){
		(a, b) = sum/count;
		return;
	}
}
(int a) = function start(float c){
	int c;
	student s = allocate student();
	s.sum = s.count-10;
	s.count = s.count + 5;
	(a) = foo(s);
	return;
}
(int a) function foo(student s){
	int c ; float d;
	student f = s;
	(d,c) = average();
	write(d);
	(a) = (c);
	return;
}
